# NMR Figure Editor 仕様

## 目的
JEOL Delta で処理したスペクトルを、資料 (Word / PowerPoint) 用の図にするまでの手作業をなくす。

| 今の課題 | 解決方法 |
|---|---|
| 不純物 (H₂O, Hexane など) のマーカーを毎回手で描く | 溶媒ごとの不純物表と照合して候補を出し、クリックで付ける。凡例も自動 |
| 貼り付けた図は画像なので後から直せない | 「データ＋描画設定」をプロジェクトファイル (`.nmrfig`) に保存し、図はそこから再生成する |
| Delta は比較した状態を保存できない | 2〜4本の重ね書き・縦並びをプロジェクトごと保存する |
| パワポ上で〇や矢印を付けている | アプリ内で楕円・四角・矢印・線・テキストを描ける。スペクトルに追従する |

## 前提
- 使うのは本人。将来は配布もありうる → サーバーを持たない静的Webアプリ。データはPCの外に出ない
  - 本番の画面には CSP (vite.config.ts) を入れ、外への通信 (fetch・画像・フォーム送信) をブラウザに止めさせる。
    connect-src / img-src は自分のサイトと data: / blob: だけ。eval も禁止 ('wasm-unsafe-eval' のみ)。
    Ketcher が使う paper.js は full 版だと読み込み時に文字列からコードを作る (中の acorn) ので core 版に差し替えている
- 入力は Delta の `.jdf` (処理済み、または未処理の FID)。別ファイルを複数読み込む
- 対象核種: ¹H / ¹³C / ¹⁹F / ³¹P、2D (COSY, HSQC, HMBC, NOESY など全部)
- 比較の用途: 反応の経時変化、原料と生成物。一度に2〜4本
- 見た目: 今の Delta の出力に寄せる (緑の線、下側に縦書きのppm値、下部にタイトル)
- 溶媒ピークの基準値は研究室の慣例に合わせて変更できる (C₆D₆ = 7.15)

## 技術構成
- Vite + React + TypeScript、状態管理は zustand + immer (元に戻す/やり直し)
- `.jdf` の読み込み: `jeolconverter` (MIT)
- 描画: 自前のSVG。画面表示と書き出しに同じ描画を使う
- 不純物表: Fulmer et al., *Organometallics* 2010, 29, 2176 の Table 1 (¹H) / Table 2 (¹³C)

## ppm軸の計算 (検証済み)
`.jdf` の先頭と末尾にはパディングがある。軸は `dataOffsetStart`〜`dataOffsetStop` の点を
`dataAxisStart`〜`dataAxisStop` に対応させて作る。

```
x(i) = axisStart + (i - offsetStart) * (axisStop - axisStart) / (offsetStop - offsetStart)
```

サンプル (C₆D₆) では残存ピークが 7.150 ppm になり、Delta の表示と一致した。
全点を等間隔に割り振る単純な方法だと 7.147 になり、0.003 ppm ずれる。

## 不純物照合
- 期待値 = 表の値 + (研究室の基準値 − 表の残存ピーク値)。例: C₆D₆ は 7.15 − 7.16 = −0.01 ppm 補正
- 許容幅: ¹H ±0.03 ppm、¹³C ±0.3 ppm (変更可)。水・OH・NH は幅を広げる (濃度・温度で動くため)
- 自動では確定しない。候補を一覧にして、ユーザーが選ぶ (例: 1.26/0.86 は Hexane とグリースのどちらも候補)
- 溶媒そのもののプロトン体 (CDCl₃ 中の CHCl₃ など) は候補から外す
- ¹⁹F / ³¹P や研究室固有の不純物 (遊離 P(OMe)₃ など) はユーザーが登録する

## マーカー
- 帰属 (本人の希望 2026-09-24「入れた構造式をマーカーで帰属したい」): マーカーの道具で ChemDraw の構造式の原子をクリックすると、
  原子の横に同じ印 (形・色) を付ける。1 つの原子に 1 つ (同じ種類なら外し、違う種類なら付け替え)。
  `Marker.imageId` + `atomId` (CDXML の原子の id、layerId は空)。原子の id は ChemDraw で直しても変わらない (ChemDraw 25 で確認)。
  置く場所は、原子から結合のない側 (結合の向きの和の逆。釣り合っていれば構造式の外側) へ、字のある原子は字の外まで (lib/cdxml.ts の cdxmlAtomSites)。
  端の原子は構造式の枠のすぐ端なので、枠の少し外 (12 px) まで原子を探す。
  ChemDraw で直して原子がなくなったら、その原子のマーカーを外して知らせる。構造式を消すと一緒に消す。凡例にも出る。
  「ChemDraw で開く」では、塗りつぶした ChemDraw の図形 (丸は楕円、ほかは直線でつないだ閉じた曲線) にして構造式と group にまとめる
- 図を開いたときから色違いのマーカーを 6 つ用意する (名前は空)。名前を入れたものだけ凡例に出す
- 不純物の照合で作られるマーカーは、これまで通り自動で名前が付く

## スキーム (サンプルごと)
- ChemDraw などからの貼り付け・ドロップに加えて、アプリ内で描ける (Ketcher。Apache-2.0)
  - 構造式・反応式を描き、SVG で保存する。描き直せるように MOL / RXN も一緒に残す
  - Indigo (WebAssembly) をブラウザの中で動かすので、外部に送信しない
  - 重いので押したときだけ読み込む (28 MB の別チャンク)。Vite では `process.env` の定義と `events` の別名が要る

## 図に置く構造式・画像
- 位置は図の左上からの割合で持つ (表示範囲を変えても動かない)。ドラッグで移動、右下の角で幅を変える (縦横比は保つ)
- アプリで描いた構造式は SVG の中身をそのまま図に埋め込む (`<g transform>` で拡大縮小)。
  Word / PowerPoint で「図形に変換」すると編集できる。描き直せるように MOL / RXN も残す
- 貼り付けた画像は data URL の `<image>` として置く (Word では編集できない旨をパネルに出す)
- 図の上で Ctrl+V: ChemDraw の CDXML は ChemDraw の構造式として置き、画像はそのまま置き、SMILES / MOL らしき文字は構造式エディタで開く

## ChemDraw の構造式 (lib/cdxml.ts, state/chemdraw.ts, lib/chemdrawExport.ts, lib/svgToEmf.ts, lib/emf.ts)
目的: 構造式と帰属を載せた図を Word に貼り、Word の上で構造式を ChemDraw で直せるようにする (本人の指定 2026-09-24)。
スペクトルは今の見た目のまま (絵) でよい。
- 取り込み: ChemDraw の「Edit > Copy As > CDXML Text」を図の上で貼る、または .cdxml をドロップ → `FigureImage.cdxml` に持つ
  - ChemDraw でふつうに Ctrl+C したものは、ブラウザで読める形がない (ChemDraw 25 で確認: CDX・OLE・EMF・SMILES などの独自の形だけで、
    文字も画像もない)。貼っても何も来ないので、Copy As > CDXML Text を案内する。.cdx (バイナリ) は読まない
  - ChemDraw の構造式を選んだまま貼ると置き換える。CDXML の座標が図の同じ位置に来るように置くので、直していない原子と帰属の印はずれない
  - 置くときは ChemDraw と同じ大きさ (Word の 96 dpi の px = 4/3 pt)
- 表示 (lib/cdxml.ts): CDXML を自前で SVG に描く。最終的に Word に入る図は ChemDraw 自身が描くので、画面で見分けがつかない程度に似せる。
  ChemDraw に描かせた画像と重ねて確かめた (scripts/cdxml-compare.mjs)
  - 拡大縮小しても線の太さ・くさびの幅・二重線の間隔は ChemDraw の書式のまま (ChemDraw で大きさを変えたときと同じ)。字は倍率に合わせる
  - 原子の文字: ChemDraw が書いた位置 (t の p) と揃え方。右揃え (LabelJustification Right) は並びを逆にする (OH → HO、NH2 → H2N)。
    Above / Below は縦に並べる。face 96 (化学式) は数字を下付き、+ - を上付き
  - 結合は文字の形の枠 (電荷の上付きは除く) + MarginWidth で止める。文字のない 2 本の結合は折れ線にして角をとがらせる
  - 二重結合の 2 本目 (DoublePosition がないとき): 環の内側 → 末端の文字の原子 (C=O) は中央 → 置換基の多い側 → 同じなら B→E の左
  - くさび: 細い側は線の太さ、太い側は BoldWidth の 1.5 倍。破線のくさびは細い側の原子から HashSpacing + 0.7×線の太さ ごと、
    長さは原子からの距離に比例 (太い側 1.8×BoldWidth)、両端の原子の上には引かない
  - すべて 1.5 の環は円。反応の矢印 (arrow)・古い形の graphic (SupersededBy のあるものは描かない)・文字・曲線・画像も描く
- 帰属の印: 構造式の上に置いた文字・丸・四角は構造式に固定する (`Annotation.imageId`、x, y は構造式の枠に対する割合、layerId は空)。
  構造式を動かす・大きさを変えると一緒に動く。構造式の外へ動かすとスペクトルに、上へ動かすと構造式に付け直す (見た目の位置は変えない)。
  線・矢印は両端が同じ構造式の上のときだけ構造式に固定する。構造式を消すと印も消す
- 書き出し「ChemDraw で開く」: 図を .cdxml にして保存する。開くと ChemDraw が起動し、Ctrl+A → Ctrl+C で Word に貼ると
  Word の上でダブルクリックして ChemDraw で直せる (ブラウザからは ChemDraw の図 (OLE) をクリップボードに置けないので、ChemDraw を 1 回通す)
  - 構造式以外 (スペクトル・軸・ピーク値など) は、画面の SVG を EMF に書き写して embeddedobject (EnhancedMetafile、16 進) に入れる。
    線のまま Word に入る (PNG だと ChemDraw が Word に渡すデータが 1 枚 10 MB ほどになる。EMF なら 1D で 700 KB ほど)。
    PNG は 16 進で入れれば読めるが base64 は読めない (ChemDraw 25 で確認)
  - ChemDraw の構造式はデータのまま、図の上と同じ位置・大きさへ移す。id は構造式ごとにずらし、色・字体は書き出す表に付け直す。
    元の文書と書式 (線の太さなど) が違えば、結合ごとに書く
  - 構造式に固定した文字は ChemDraw の文字 (InterpretChemically="no") にして、構造式と group にまとめる
  - 色の番号: 0 = 黒、1 = 白、colortable の 1 つ目が 2
- ChemDraw で描く (本人の希望 2026-09-24「アプリからケムドロ作成」「保存先は NMR の保存先でよい」「すぐ開いて編集。Word に貼った
  ChemDraw のように保存しなくてよいように」、state/chemdraw.ts + tools/chemdraw-link/):
  - ブラウザからは ChemDraw を起動できず (chemdraw: のリンクは Signals のサインイン用)、描いている途中の中身も見えない。
    そこで、この PC に連携を 1 回だけ入れてもらう (管理者の権限は要らない)。
  - 連携の準備 (本人の希望 2026-09-24「初回と設定に ChemDraw リンクボタン、フォルダは勝手に用意してよい」):
    初めて開いたときの説明の 5 枚目・設定・構造式ボタン (連携がまだのとき) の「ChemDraw と連携する」→
    NMR の保存先 (書き込みの許可を取る) に「ChemDraw」フォルダを作り、ChemDraw連携を入れる.cmd を置く (lib/linkInstaller.ts)。
    本人がダブルクリックすると、置かれた場所から NMR の保存先がわかるので、フォルダは聞かない (保存先の食い違いが起きない)。
    ブラウザが書かせてくれないときはダウンロードにし、開いたときに保存先を選んでもらう
  - ChemDraw連携を入れる.cmd: ASCII だけの cmd。起動の行が自分を読み、最後の ::NMRFIG:: のあとの base64 (installer.ps1 と、
    helper.ps1・launch.vbs の base64) を PowerShell で実行する。改行は CRLF にそろえる (公開版は Linux でビルドするため)
  - 連携 (tools/chemdraw-link/installer.ps1): %LOCALAPPDATA%\NMRFigure\chemdraw-link に helper.ps1 と launch.vbs と config.json
    (NMR の保存先) を置き、nmrfig-chemdraw: を HKCU\Software\Classes に登録し、NMR の保存先\ChemDraw\.nmrfig-link.json を置く。
    アプリはこの印で「連携できた」と知る (2 秒ごとに見て知らせる。設定にも出す)。同じフォルダに入っていれば、入れ直すか外すかを聞く。
    環境変数 NMRFIG_DRYRUN があればレジストリに触らず %TEMP% に書く (試験用)。
    このリポジトリの tools/chemdraw-link の .bat からも入れる・外すができる (そのときは NMR の保存先を聞く)
  - 構造式ボタン (初めて押したときにどちらで描くか聞く。設定で変えられる)・ChemDraw の構造式のダブルクリック・「ChemDraw で直す」→
    `nmrfig-chemdraw:open?name=structure-<id の 16 進 16 字>&data=<CDXML を deflate して base64url>` を開く。
    連携 (helper.ps1) がそれを NMR の保存先\ChemDraw\ に書き、開いている ChemDraw の中に開く (なければ起動)。
    描いている間は 0.9 秒ごとに ChemDraw の中身 (Objects.Data chemical/x-cdxml) を読み、変わっていればファイルに書いて
    「変更あり」を消す (閉じるときに保存を聞かれない)。書類を閉じたら終わる。同じ構造式の連携がすでに動いていれば、前に出すだけ
  - アプリは NMR の保存先 (ホーム画面で開くフォルダ、読み取りの許可) の ChemDraw フォルダのそのファイルを 1 秒ごと (とアプリに戻ったとき) に見て、
    変わっていれば図に入れる。新しい構造式は、白紙でなくなった最初の変化で置く (id はファイル名と同じもの)。
    描いている間の変化は 4 秒ごとにだけ「元に戻す」の区切りを作る
  - 12 秒たってもファイルができなければ、連携が入っていないか、連携の NMR の保存先がアプリのフォルダと違うと案内する
  - ChemDraw フォルダの片付け (本人の希望 2026-09-24「要らなくなったファイルは随時消す」、sweepChemDrawFolder):
    連携 (版 2) は書類を閉じると structure-….closed を置く。アプリは最後の中身を図に入れてから .cdxml と .closed を消す。
    ほかに、連携が入ったあとの ChemDraw連携を入れる.cmd、1 分より古い .tmp、ファイルのない .closed を消す。
    開いている図の構造式のファイルが残っていれば (アプリを開き直した・別の図にしていた)、中身を図に入れて見張り直す。
    ほかの図の構造式のファイルは、その図を開き直したときに拾えるよう 12 時間残してから消す。連携の印は残す。
    起動して 3 秒後・30 秒ごと (画面を出しているとき)・アプリに戻ったとき・描いている間は 10 秒ごとに行う。
    消すのは書き込みの許可があるときだけ (ChemDraw で描くときに許可を取る)。
    印の version が 1 (前の版の連携) なら、設定に「連携し直すと片付くようになる」と出す
  - 連携は structure-16進 の名前・CDXML の中身だけを扱う (ほかのサイトから呼ばれても、決まったフォルダの決まった名前のファイルにしか書かない)。
    リンクの長さは Windows のコマンドの上限 (32,767 字) に収める (構造式 1 つで 2,000 字ほど)

## 注釈 (パワポ風の図形)
- 種類: 楕円・四角・矢印・線・テキスト。色・線幅・破線・塗りを変更できる
- 位置は「どのスペクトルの何ppm・どの高さ」で保存する → 拡大・並べ替え・基準合わせをしてもピークからずれない
  (構造式の上に置いたものは構造式に固定する。上の「ChemDraw の構造式」)
- マーカー: 凡例の項目 (不純物・生成物など) を選んで、ピークをクリックして付ける

## ピーク値 (ピークピックアップ)
- 自動で拾う: 表示範囲 (または全体) で、最大ピークの n% (既定 5%) とノイズの 5 倍のうち高い方を超える極大すべて。
  同じピークかどうかはデータ点の間隔で見るので、多重線の隣の線は別のピークとして残る
- 一覧 (peaks.ts) は今のデータから頂点の高さを読み、一番高いピークを 100% として出す。タブ区切り (Excel 用) と値だけの形でコピーできる

## ピーク値ラベルの配置
- 「軸の下」: Delta と同じく、x 軸の下に縦書きで並べ、引き出し線でピークとつなぐ
- 積み重ねのときは、軸の下に出すのは一番下のスペクトルだけ。ほかはピークの上に出す (どのスペクトルの値か分かるように)
- ピークの上にラベルがあるスペクトルは、ラベルの分だけ低く描いて重ならないようにする

## Delta の注釈 (.jdf の中のピーク値・積分)
JEOL の公開仕様ではないので、研究室の実データ 242 ファイル (注釈つき 114) から読み解いた
(読む: lib/jdfAnnotations.ts、書く: lib/jdfWrite.ts、決まりの確かめ: lib/jdfAnnoteCheck.ts)。
- ヘッダの annoteStart (uint64 @1308, BE) / annoteLength (uint32 @1316, BE) が場所を指す。
  場所は常に context の直後 (64 バイト境界とは限らない)。ヘッダ @1203 の最上位ビット (annotationOK) はどのファイルも立っている
- 中身は 352 バイトの見出し + 336 バイト固定長レコード × 枠数 (枠数は見出しの +8、+12 = (枠数+1)×336、先頭は 336)
- レコード (リトルエンディアン):
  | 場所 | 中身 |
  |---|---|
  | +0 float64 | ppm (積分は範囲の中心)。空き枠では uint32 で「次の空き枠」 |
  | +64 / +66 … uint16 | スキップリストの「次」(段 0 / 段 1 …、1 始まりの枠番号、0 = 終わり) |
  | +82 uint8 | 種類。1〜3 = ピーク値、64〜67 = 積分 (最下位ビット = 選ばれている印)。72/74/10 は別の注釈 (このアプリでは扱わない) |
  | +87 uint8 | ピーク 0x02 / 積分 0x12 |
  | +88 float64 | ピークの高さ (その点のデータ値) |
  | +96 float32 | ピークの半値全幅 / 積分の幅 (範囲 = 中心 ± 幅/2) |
  | +128 float64 | 積分の生の値 = 範囲の強度の和 − 点数 × (+328)。**406 件すべてで計算の誤差まで一致** |
  | +136, +316 float64 | Delta の画面の値 = 生の値 × 見出し +224 |
  | +144 float64 | 積分のベースラインの傾き (1 ppm あたり) = (高い ppm 側の端 − 低い側の端) / 幅 |
  | +152 / +154 uint16 | 積分どうしの双方向の鎖 (1 つ新しい積分 / 1 つ古い積分) |
  | +328 float64 | 積分のベースラインの高さ (中心で) = 両端の平均 |
- 端の高さは **端の点を中心に 11 点の平均**。Delta で手で直していない積分 365/406 件でこの決め方と一致 (残りは手で直したもの)
- 見出し: +20 = スキップリストの段の数 / +52 + 2L = 段 L の先頭 / +222 = 一番新しい積分 / +220 = 選ばれている注釈 /
  +120 = 空き枠の鎖の先頭 (空き枠が無いファイルは多くが +120 = +122 = 0) / +224 = 積分の倍率 / +248 = 「この積分を n にする」の n
- スキップリストは **ppm の大きい順**。114 ファイルすべてで、各段が並んでいて輪がなく、上の段は下の段の一部
- **同じ ppm は鎖に 2 つ入らない**: ピークと中心がまったく同じ積分は、Delta 自身が鎖から外している (16 件、積分の鎖には入っている)
- 積分の範囲は ppm ± 幅/2。両端はデータ点の上 (Delta で 1〜2 ppm と 7〜8 ppm に引いてもらったファイルで確認)

## Delta への書き戻し (lib/jdfWrite.ts)
2026-09-19 の版は Delta が開けず断念した。**2026-09-23 に原因が分かった**: +64 を「通し番号」と思い込んで書いていたが、
実際はスキップリストの「次」だった。枠 1 に 1、枠 2 に 2 と書いたため、**レコードが自分自身を指す輪**になり、
Delta が鎖をたどり終わらずに固まっていた。ピークだけのファイルが開けたのに何も出なかったのも、見出し +52 (先頭) が 0 だったため。
積分の +152 にも見本の値 (2) が残っていて、積分の鎖も輪になっていた。

今の書き方:
- 元の .jdf を丸ごと複製し、注釈の場所 (annoteStart 以降) だけ作り直す。測定データ・パラメータには触らない
- 注釈のうしろに 0 以外があるファイルは書き換えない。注釈の場所が無いファイル (処理済みの 7%) は、Delta の空の見出しから作る
- 枠はちょうどの数 (空き枠なし、+120 = +122 = 0)。スキップリストは 1 段 (+20 = 1)。ピーク → 積分 (古い順) の順に並べる
- ppm は必ずデータ点に寄せる。同じ ppm はピークを優先し、積分は Delta と同じく鎖から外す
- 積分は Delta のベースライン (取り込んだものはそのまま、アプリで作ったものは 11 点の平均) で生の値を出し、
  基準の積分からの倍率 (+224) と値 (+248) を書く → **Delta の画面にもアプリと同じ数字が出る**
- 書いたあと jdfAnnoteCheck の決まり (Delta の 114 ファイルがすべて満たすもの) で確かめ、満たさなければ書かない
- ピーク値・積分のほかの注釈 (種類 10 / 72 / 74) は中身をそのまま写し、つながり (+64〜+74) の番号だけ付け替える。
  それらは互いにだけ指し合う (まれに +68 で積分を指す: 研究室で 3 件 → 書き直した同じ範囲の積分を指すようにする)。
  ピーク値・積分の側からそれらを指すことはない
- 全部消したときは、Delta が空の注釈を書くときと同じ形 (空き枠 4、+120 = 4 → 各枠の +0 が 1 つ前、+122 = 1) にする
- 検証: Delta が積分を 2 つ書いたファイルと、同じ積分をこちらで書いたレコードは、種類・印・ppm・幅・つながり・生の値・ベースラインが一致。
  研究室の注釈つき 106 ファイル (うち 45 はほかの注釈つき) を「読む → 書く → 読む」で往復して、範囲・値・ほかの注釈が戻る
- 実機の Delta で開けることを確認済み (2026-09-23: 積分だけ・ピーク値だけ・両方・注釈つきの書き直し・注釈の場所が無いファイル)。
  ほかの注釈を残す書き方 (v3) は確認待ち

## 図を .jdf として保存する (lib/jdfSections.ts, lib/jdfEmbed.ts, lib/jdfProcessed.ts)
2026-09-24 本人の希望: 「保存したものも Delta の形式にしたい。フォルダでダブルクリックしたら Delta で開く」
(ファイルを 1 種類にしたい / 図の情報は .jdf の中に / 重ね書きもできれば .jdf、無理なら .nmrfig をこのアプリで開けるように /
このアプリで FID から処理したものも Delta で開ける .jdf に)。

**手がかり: この PC には Delta 6.5 が入っていて、付属の道具で Delta 自身の読み方を確かめられる**
(`C:\Program Files\JEOL\Delta 6.5.0.app\Contents\WinNT\`)。
- `list_header.exe +a +c +p ファイル` … ヘッダ・注釈 (項目名つき: AT_HEADER / AT_ASSIGNMENT、ml_text、int_list …)・文脈の名前・パラメーターを出す
- `convert.exe -delta -file 入力 -delta -file 出力` … Delta の読み書きの部品で読み、書き直す (出力は「出力-1.jdf」)。
  文脈は PARAMETER_STORAGE だけ残る (画面での保存とは違う)。フルパス (Windows の書き方) で渡す

.jdf の並び (234 ファイルすべて同じ): ヘッダ 1360 バイト → パラメーター (1360〜) → 0 埋め → 測定データ (16384〜) → 文脈 → 注釈 →
64 バイト境界まで 0。total_size = ファイルの大きさ。区画に分けて組み直すと元と 1 バイトも違わない (jdfEmbed.test.ts)。
- **文脈 (context)** は [種類 u32][長さ u32][中身] の並びで 16 バイト境界。種類の名前 (list_header +c):
  1 = EXPERIMENT_SRC (パルスプログラム)、2 = PROCESSING_LIST (当てた処理)、3 = UNAPPLIED_PROCESSING_LIST (これから当てる処理。生データだけ)、
  15 = FILE_VIEW、16 = 一覧表 (INTEGRAL LISTING。1 件だけ)、33 = PARAMETER_STORAGE
- **PARAMETER_STORAGE** は 16 文字に収まらない文字のパラメーターの全文の置き場。キーはパラメーター名の大文字 (SAMPLE_ID、EXPERIMENT_PATH …)。
  中身はビッグエンディアンの [タグ][数] で、0x2b2a = リスト、0x271d = 文字 (長さ + 文字)、0x2725 = 真偽
- **パラメーター**は 64 バイトの記録: +1 群の中の番号 / +2 群 (0x2b = DEV_USER_ANALYSIS) / +16 値 (16 バイト、文字は空白埋め) /
  +32 値の形 (0 = 文字) / +36 名前 (28 文字)

**図の入れ場所**: 名前 `nmrfig_info` / `nmrfig_data_001…` の文字のパラメーター (群 0x2b) を足し、全文を PARAMETER_STORAGE に置く。
中身は .nmrfig と同じ JSON を gzip → base64 → 64 KB ずつ。info = "v1 gzip 分けた数 文字数 crc32"。
試した置き場と convert.exe で読み書きし直したあと:
| 置き場 | Delta で読める | 書き直したあと |
|---|---|---|
| 注釈のうしろ (total_size に含める) | 読める | 消える |
| 知らない種類 (99) の文脈 | 読める (名前は空) | 消える |
| PARAMETER_STORAGE に組だけ足す | 読める | 消える (パラメーターのない組は捨てられる) |
| **パラメーター + PARAMETER_STORAGE** | 読める (list_header +p に全文が出る) | **1 文字も変わらず残る** (200 KB を 1 つでも 25 に分けても) |

**FID から処理したものを処理済みの .jdf にする** (Delta の処理前 -1-1 と処理後 -1-2 の 113 組を比べた):
- ヘッダの違いは、軸の単位 (秒 → ppm、+33 = 26)・点の数・有効な点の範囲・軸の両端・base_freq・zero_point と区画の長さだけ。
  パラメーターは処理の結果 (machinephase_x_p0 など 11 個) が増えるだけで、ほかはそのまま。文脈は 3 が消えて 2 が増える
- データは 64 bit の複素数 (実部を全部、続けて虚部)。点の数は 16 の倍数に 0 で埋め、前に半分 (切り上げ) を置く (13107 点 → 13120、7〜13113)
- base_freq = 0 ppm の周波数 × (1 + (x_offset − 基準合わせでずらした ppm) × 10⁻⁶)。zero_point = ずらした ppm × base_freq (Hz)。
  Delta の 1H で ずらした量 0.0118 ppm → zero_point 4.72、0.0586 → 23.41 (どれも一致)
- このアプリの書き方: 元の FID の .jdf を複製し、ヘッダの軸とデータを差し替え、文脈の 3 を外し、注釈は空。
  強度はこのアプリと同じ値のまま (Delta は最大 1 前後にそろえるが、そろえると Delta と行き来する積分のベースライン (+328) の単位が図と合わなくなる)。
  同じ FID を Delta が処理したファイルと、一番高いピークの位置が 0.005 ppm 以内・zero_point が一致 (jdfEmbed.test.ts)
- **軸には基準合わせのずれ (refOffset) が入る**。図のピーク値・積分はずれの前の軸 (データの軸) で持っているので、
  書くときは +refOffset、読むときはファイルの軸の始まり − 図の first だけ戻す (lib/deltaSync.ts の fileShift / shiftAnnotations)。
  これを忘れると Delta では基準合わせの分 (1H で約 0.06 ppm) ずれた所にピーク値が出る (テストで 1 点ずれて見つかった)

Delta の画面で開いて確かめるファイル: samples/delta-test-v4/ (A = Delta で処理した 1H に 2 本重ねた図を入れたもの、
B = このアプリで処理した 1H + ピーク値・積分 + 図、C = このアプリで処理した 13C + ピーク値 + 図)。どれも convert.exe を通しても図が残る。
**2026-09-24 本人が Delta の画面で開けることを確認**。Delta の画面で上書き保存したあとも図が残るかは確認待ち。

**アプリへのつなぎ込み (lib/figureJdf.ts, state/fileOps.ts)**:
- 保存 (Ctrl+S): 1D で土台にできるスペクトルがあれば図入りの .jdf。土台 = 図の一番下に描いているスペクトル (titleText と同じ。文献・見えないものは後回し)。
  土台の中身は、読み込んだときの .jdf (store の sources)、なければデータフォルダ・覚えているファイルから読む。見つからなければ .nmrfig に落とす
- 保存先は新しいファイル (元の測定ファイルは上書きしない)。名前の下書きは「土台のファイル名_図.jdf」、ダイアログは土台と同じフォルダで開く
- 保存したあと、Delta で処理した土台は **その図の .jdf と同期する** (SpectrumMeta.syncFile)。同期の相手を変えるのはファイルを書けてから
  (先に変えると、まだ空のファイルを読みに行く)。重ねたほかのスペクトルは元のファイルと同期したまま
- FID から処理した土台も、図入りの .jdf に保存したあとは自動で同期する (2026-09-24 本人の希望)。まだ保存していない FID は同期しない。
  - ピーク値・積分は Delta で処理したものと同じに行き来する (ファイルの軸とのずれは fileShift で戻す)
  - このアプリで処理 (位相・LB・ベースライン・基準) を変えると、データも書き直す (Link.dataKey = dataSignature。変わったら push)
  - Delta で処理し直して保存されたら (ファイルが変わっていて、データの形が今の処理と違う)、Delta のスペクトルに切り替える
    (processing = null、refOffset = 0、FID は捨てる)。以後は Delta で処理したスペクトルと同じ扱い
  - この図を閉じている間にこのアプリの処理が変わっていた (ファイルは前と同じ・データの形が違う) ときは、開いたときにデータを書き直す
- ファイルに書けない開き方 (ハンドルがない) のときだけ、開いたときにファイルのピーク値・積分が図と違えば「Delta の中身を入れる / 図のまま」を聞く
- 開く: パラメーター nmrfig_info があれば図ごと開く (図を編集中に足そうとしたときは「図として開く / スペクトルだけ足す」を聞く)。
  土台の syncFile は開いたファイルの名前にする (名前を変えたり写したりしたファイルでも、開いたファイルが相手)
- 図の中身 (.nmrfig と同じ JSON) に jdfBase (土台の spectrumId) を入れる。一覧用に nmrfig_summary = "本数|核種+核種" (16 文字以内なので記録だけ)
- ホーム画面 (2026-09-24 本人の希望: 「図」の行を下に増やさず、編集後のものを版として。すっきりしたホーム画面を保つ):
  保存した図は、土台のスペクトルの元の測定の「編集した版」にまとめる。札は 1 つで「編集」「2 版」、開くと編集した版 (図) が開く。
  ほかの版 (Delta で処理した版・生データ) は右の「ファイル」で選ぶ
  - データフォルダの図入りの .jdf: パラメーター・タイトルは元の .jdf を写したものなので、groupMeasurements の測定の id
    (サンプル名・測定時刻・次元・核種・パルス) が元の測定と同じになり、そのまままとまる。版の並びは編集した版 → 処理版 → 生データ
  - ブラウザの中に残した図 (.nmrfig やフォルダの外に保存したもの): SavedFigure.base (土台の元の .jdf の名前) で元の測定を探し、
    その測定の情報を写した仮の項目 (key = saved:id、savedFigureId、baseKey) を一覧に足す (library.ts の listedFiles)。
    base の無い古い図は、読み込むときに中身から補う (baseOfJson)。同じ名前の図入りの .jdf がフォルダにあれば、そちらだけ出す
  - 元の測定がフォルダにない図だけ、サンプルのカードに図の札のまま出す (unlistedFigures)
  - 保存先がデータフォルダの中なら、保存したあと一覧を読み直す
- 画面での確認 (5180、偽の保存ダイアログ): 2 本重ね → 保存 → 開き直しで層・積分・色が戻り、土台は図のファイルと同期済み。
  13C (FID) → 保存 → Delta でピークを足したことにして開くと、取り込むか聞く。list_header / convert.exe でも読めて図が残る

## 画面の言語 (src/i18n) と使い方の説明 (components/Onboarding.tsx)
2026-09-24 本人の希望: 「初回起動時に使い方説明をオンボーディングしたい」「英語対応して」。
- **書き方**: 日本語の文をそのまま鍵にして tr('ファイルを開く')。英語のときは src/i18n/en/*.ts の辞書を引き、無ければ日本語のまま。
  値は tr('{name} を開きました', { name })、英語の複数形は訳の側で {n|measurement|measurements}。文の途中に部品 (太字・Kbd) が入るときは trx
- 読み込み時に作る表 (道具の一覧・並び順の名前・説明のページ) は trk('…') で印だけ付け、画面に出す所で tr(変数)
  (読み込んだ時点ではまだ言語が決まっていないため)。関数の名前は tr (t は変数名とぶつかる所が 19 か所あったため)
- 言語は settings.ui.lang (auto / ja / en)。auto はブラウザの一番目の言語が日本語なら日本語、ほかは英語。
  切り替えると App を key で作り直して全部描き直す。テスト (Node) は日本語のまま (setLang を呼ばない)
- 図の中の文字: 文献の引用の行の「文献値から作図」は言語に合わせる。図のそのほか (軸・タイトル) はもともと英語
- 訳さないもの: 検索のよみがな (CommandPalette の READINGS・keywords)、「日本語」という言語名、日本語の日付の書き方、
  前の版の文献の名前「文献 (…)」の判定、SI 用テキスト (もともと英語)
- 書き換えは TypeScript の構文解析で一度にやった (日本語の文字列・JSX の文字・`${…}` を含む文、839 の文)。
  そのあと、文の途中で切れた断片 (「(保存」「{n} 本」など) を手で 1 つの文にまとめ直した
- 確かめ: src/i18n/i18n.test.ts がソースの tr/trk/trx の文をすべて拾い、英語があるか・差し込む名前が同じか・使われていない訳がないかを見る。
  画面は `UI_LANG=en node scripts/ui-shots.mjs` (?lang=en。開発のときだけ効く) で撮って確かめる
- 使い方の説明: 5 枚 (ようこそ + 言語 / データフォルダ / 図を作る / Delta と行き来 / Word・PowerPoint)。本当にある操作だけを書く。
  初めて開いたとき (settings.ui.onboardingDone が false) に出し、閉じると覚える。ホーム画面の「使い方」・設定・Ctrl+K から見直せる。
  画面の見本は ?demo=onboarding (それ以外の ?demo では出さない)

## アプリとして入れる (public/manifest.webmanifest, state/launch.ts)
2026-09-24 本人の希望: .jdf にできない図 (2D・文献だけ) の .nmrfig も、ダブルクリックでこのソフトが開くように。
- Web App Manifest (name・192/512 の PNG アイコン・start_url・display standalone) で Edge / Chrome に入れられる。
  Edge を DevTools (CDP) で動かし、Page.getInstallabilityErrors が空・manifest のエラーなしを確かめた。サービスワーカーは使わない
- file_handlers で .nmrfig だけを受け持つ (.jdf は Delta のまま)。launch_handler は focus-existing (開いている窓に渡す)
- 渡されたファイルは launchQueue.setConsumer で受け取り、openFiles(…, 'new') で開く。前回の作業を読み込んだあとに始める
  (先に開くと、前回の図の読み込みで上書きされる)
- アイコンは design/app-icon.svg (ファビコンと同じ絵柄) を scripts/make-icons.mjs が Edge で描いて public/icon-192.png / icon-512.png にする
- CSP の default-src 'self' で manifest も読める。配布用の serve.ps1 に .webmanifest の種類を足した

## Delta との同期 (state/deltaSync.ts, lib/deltaSync.ts)
2026-09-23。ボタンで書き出すのではなく、図と .jdf を自動で行き来させる (本人の希望: 「勝手に相互編集」)。
Delta も「同じファイルに上書き」で保存するので、このアプリも開いた .jdf に上書きする。
- 対象: 図のスペクトルのうち、Delta で処理済みの 1D (.jdf、FID を処理したもの・文献のものは除く)。同じファイルが 2 本あれば最初の 1 本
- ファイル: データフォルダを読み込んだときのファイルハンドル (library.ts)、またはドロップ・「開く」のハンドル。
  書き込みはフォルダの readwrite の許可が要る (編集の直後ならその場で聞く。だめなら「書き込みを許可する」ボタン)
- 中身の比較は「鍵」: ピークはデータ点、積分は両端の点と Delta の画面の値 (5 桁)。点の間の ppm や、基準の積分の選び方の違いでは食い違わない
- このアプリ → Delta: 図が変わったら鍵を比べ、違えば 1.5 秒待って書く。書く直前にファイルを読み直し、
  前に合わせたあと Delta でも保存されていたら、あとから変えた方を使う (ファイルの更新時刻 と このアプリで変えた時刻)
- Delta → このアプリ: 4 秒ごと (画面が見えているとき) と、画面に戻ってきたときにファイルの更新時刻を見る。変わっていれば読み、
  中身が違えば図に入れる (同じ点・同じ範囲のものは id と SI テキストの上書きを残す)。スペクトルも違えば読み直す
- 図ごとに「最後に合わせた時点」(更新時刻と鍵) を IndexedDB の sync に置く (doc.id + ファイル名)。元に戻す/やり直しの対象にしない
  - 開いたとき、ファイルだけ変わっていれば読む・図だけ変わっていれば書く・両方なら新しい方
  - 記録が無い (この機能より前の図) で、図にもピーク値・積分があって中身が違うときは、どちらに合わせるか一度だけ聞く。
    図が空なら黙って読む (ファイルは書き換えない)
- Delta で開いたままの画面は外から書き換えられない (Delta 側で開き直すと反映される)

## 記録 (state/history.ts)
- **自分で付ける記録** (2026-09-23 本人の希望: 「任意のタイミングで、メモは任意で。毎回付けていたらきりがない」)。
  「記録を付ける」で今の中身を残す (manual、memo)。一覧の中心はこれ。メモはあとから直せる・記録は消せる。自動では消さない。
  Delta と同じ中身のときは注釈の場所 (block) も残す。Delta と同期しないスペクトル (FID を処理したものなど) にも付けられる (戻すのは図だけ)
- **自動の控え**: .jdf (ファイル名) ごとに、同期のたびの中身を残す (上書きされた中身を戻すため)。画面では畳んでおく。
  Delta で保存・このアプリで書いた・書く前のファイル・使わなかった変更・記録から戻した、を出どころつきで
- Delta が書いた中身は注釈の場所をそのまま (block) 残し、戻すときはそのまま書く (Delta が書いたとおり、ほかの注釈も含めて戻る)
- 初めて書き込む前のファイルは丸ごと残す (originals)。「このソフトで書き込む前のファイルに戻す」で戻せる
- 自動の控えは、直前の控えと同じ中身なら足さない。開いただけのときは、同じ出どころの直近と同じなら足さない。
  1 ファイル 100 件まで (古いものから消す。最初の 1 件と自分で付けた記録は消さない: pruneHistory)
- 置き場所は IndexedDB (history / originals / sync、DB の版 4)。navigator.storage.persist() で消さないよう頼む
- 図に出したくないときのために「図に入れるもの」に「ピーク値」「積分」を足した (消すと Delta からも消えるので、隠すだけ)

## 簡易リッチテキスト
- `^{..}` 上付き / `_{..}` 下付き / `*{..}` 斜体 / `**{..}` 太字 (richText.ts)
- SVG では tspan の baseline-shift・font-style・font-weight で書く。Word/PowerPoint の「図形に変換」でも書式として残る

## PowerPoint の「図形に変換」への対応 (検証済み)
PowerPoint 365 で実際に変換して確かめた結果、次の書き方にしている。
- 上付き・下付きは `baseline-shift` (割合) + `font-size="70%"` で書く。`dy` で上下させると位置がずれて崩れる。
  この書き方だと PowerPoint でも本物の上付き・下付きの書式になる
- 回転した文字に `dy` を付けると、PowerPoint で文字が大きくなる。中心合わせのずれは `translate` に入れる
- 確認用: `scripts/ppt-svg-check.ps1`

## 図のコピー (Word / PowerPoint)
- クリップボードに `image/svg+xml` と `image/png` を両方入れる。Word 365 / PowerPoint 365 は SVG を優先してベクターの図として貼る
  (Word の InlineShape の種類が 17 = SVG になることを確認。PNG だけだと Word が圧縮して荒くなっていた)
- `ClipboardItem.supports('image/svg+xml')` が false のブラウザでは PNG (既定 4 倍) だけを入れ、その旨を表示する
- 確認用: `scripts/word-clip-check.ps1`

## FID の処理 (Delta の処理結果と比べて検証済み)
- 虚部の符号を反転する (しないと Delta と鏡像になる)
- DC 補正 (後ろ 10% の平均) → 指数関数の窓 → ゼロ詰め 2 倍 → FFT → 中央 `x_sweep_clipped / x_sweep` だけ残す
- ppm = x_offset + Hz / refMHz、refMHz = x_freq / (1 + x_offset·10⁻⁶) / 10⁶
- 装置の遅れを 1次位相として先に戻す。時間の原点のずれ = デジタルフィルターの遅れ − 受信の遅れ (点)
  - フィルターの遅れ: パラメーター `orders` / `factors` から (jeolconverter と同じ式。約 19.66 点)
  - 受信の遅れ: `acq_delay` × `x_sweep`。これを入れないと ³¹P (142 kHz 幅) で 1次位相が 840° 近くずれる
- 残りの位相の自動補正
  - ¹H: ACME (1次微分のエントロピー + 雑音の 3 倍を超える負の値への罰則) を 0次 → 0次・1次の順に最小化。1次位相は ±半周期まで
  - ¹³C・³¹P・¹⁹F: 溶媒が飛び抜けて大きくピークがまばらなので、ACME では 1次位相を大きく外す (¹³C の脂肪族が分散形になった)。
    孤立したピーク (ノイズの 20 倍以上、半値幅の 20 倍の範囲で両側が 5% まで下がる) の頂点の位相に、
    12° 以内に乗るピークが最も多い直線を2点から選び (RANSAC)、乗ったピークで当て直して 1次位相とする。
    広がりが全幅の 5% 未満なら 1次位相は 0。0次位相はその 1次位相のもとで ACME を最小にする
  - 1次位相の中心は一番大きいピーク。1次位相の値は「表示範囲の全幅で回る角度」
- ベースライン補正: 1024 区間の中央値にルジャンドル多項式 (5 次) を当て、上に出た点を多項式の値に置き換えながら 30 回繰り返す
- 基準合わせ: 研究室の基準値 ±0.1 ppm (¹H) / ±0.5 ppm (ほか) にある、最大の 20% 以上の線のうち基準値に一番近いもの (¹³C の三重線は中央)
- 検証: Delta の生データと処理版の組 68 組で、Delta の図との相関 ¹H 平均 0.991、¹³C 0.988、¹⁹F 0.962、³¹P 0.875
  (³¹P の低い組は Delta 側の位相がずれていたもの)。¹H の基準のずれは Delta と同じ +0.0585 ppm
- 生データはプロジェクトファイルに保存し、位相・LB・ベースラインの変更はそこから計算し直す (元に戻す/やり直しの対象)

## 推移グラフ
- 追跡する範囲 (ppm) ごとに、両端を結んだ直線をベースラインとして面積 (または高さ) を求め、nH で割る
- 縦軸: 合計を100% / 基準の範囲との比 / 最初の時点を100% / そのまま
- 時間はスペクトルごとに入力。未入力なら名前の最初の数値、それもなければ並び順

## ホーム画面
- フォルダの許可が `denied` のときは requestPermission がダイアログを出さずに拒否を返す (押しても何も起きないように見える)。
  この状態はボタンを「フォルダを選び直す」にし、押したら選び直しのダイアログを出す
- 並びは「測定日 / 名前 / 編集順 (ファイルの保存日時)」× 降順・昇順。設定 (localStorage) に覚える
  - 測定日: 日ごとの見出しを出し、日の中はサンプルを測定時刻順に並べる
  - 名前・編集順: 日の見出しを出さず、同じサンプルの測定を1枚のカードにまとめて並べる (カードに測定日を出す)
- データフォルダは1つ (File System Access API で選び、IndexedDB に覚える)。サブフォルダは読まない
- `.jdf` はヘッダーとパラメーターだけ読む (数十 KB)。結果は IndexedDB にキャッシュ (名前・サイズ・更新日時がキー)
- 測定日時は `actual_start_time` (1990-01-01 UTC からの秒)。jeolconverter のヘッダーの作成日は日がずれるので使わない
- 並びは 測定日 → サンプル (Delta のタイトル) → 実験。スキーム画像・メモ・タグはサンプルごとに IndexedDB に保存

## 積分
- 範囲の強度の和 × 点の間隔。範囲は基準合わせ前の ppm で持つ。両端はそれぞれ一番近いデータ点 (Delta と同じ)
- **両端を結ぶ直線を引いてから足す** (figure.integralBaseline、既定 入)。直線は Delta と同じく「端の点を中心に 11 点の平均」を結ぶ。
  引く分 = 直線の中央の高さ × 点数 (傾きの分は打ち消し合う)。Delta の値と計算の誤差まで一致する (上の「Delta の注釈」)
- Delta から取り込んだ積分は、Delta のベースライン (手で直したものも) をそのまま持つ (Integral.baseline)。範囲を変えたら消して 11 点の決め方に戻す
- Delta の値のそろえ方 (倍率) も取り込む: 見出し +248 と画面の値が合う積分を基準にする (無ければ最初の積分を、その画面の値で)
- 自動積分 (積分パネルの「自動で積分」): detectSignals を核種によらず使う。高さの下限 (既定 3%) と範囲 (全体 / 表示範囲) を選べる。
  すでにある積分と重なる範囲は作らないので、押し直しても手で引いたものは残る
- スペクトルごとに基準 (integralRef) を1つ持ち、その値に比例してほかの値を出す。基準がなければ最初の積分 = 1
- 値の位置: 曲線の上 (Delta 風) / 軸の下にかぎ括弧。積み重ねのとき、軸の下に出すのは一番下のスペクトルだけ

## ピークの高さ
- 小さいピークを見せたいときに全体を引き上げる操作なので、ツールバーに専用の道具を置く (高さツール H)。上下にドラッグで全体の縦倍率、Shift でそのスペクトルだけ。ホイールだけでも変わる
- ツールバーの ↑ ↓ ボタンで 1.5 倍ずつ。タッチだけでも使えるようにするため (Shift やホイールが使えない)
- 選択ツールで背景をドラッグしたときは、最初に動いた向きで決める: 左右 = 表示範囲の移動、上下 = そのスペクトルの倍率 (Shift で全体の縦倍率)
- 上に飛び出した線はプロット領域で切る (Delta と同じ見え方)

## 2D (COSY・HMBC など)
- この研究室のデータに Delta で処理した 2D はないので、生データ (両軸とも Second) だけを読む
- 処理: サインベル窓 → ゼロ詰め → F2 の FFT → 列ごとに F1 の FFT → 絶対値。
  絶対値は位相に影響されないので、位相補正もデジタルフィルターの遅れの補正も要らない (COSY・HMBC の標準的な見せ方)
- F1 は列の複素数を共役にしてから FT する。しないと搬送波を中心に反転した像になる (実データの対角線で確かめた)
- 等高線: 表示範囲を画面の細かさ (1px あたり 1.5 点) に取り直してからマーチングスクエア法。
  縮めるときは最大値でまとめ (弱いピークを消さない)、拡大したときは間を補う
- 等高線の下限の初期値は雑音の 6 倍 (中央値を雑音とみなす)。雑音ばかりのデータでは線が数十万本になるので、
  1本あたり 4万線分を超えたらその高さは描かずに画面で知らせる
- 図は正方形にする (対角線が 45°)。上と右に投影 (表示範囲の最大値)、同じ核種なら対角線を点線で出す
- 2D と 1D は同じ図に混ぜない。図形・ピーク値ラベル・積分・推移グラフは 2D では出さない
- プロジェクトファイルには生データだけを入れ、開くときに FT し直す (処理後の行列は大きいため)

## 保存
- 作業中の図 (doc・スペクトルの中身・保存先のファイル) は IndexedDB に自動保存する。1.2 秒の遅延と、画面を隠したときに書く
  - doc は小さいので変更のたび、スペクトルの中身は開いているものが変わったときだけ書く
  - 起動時に読み戻し、ホーム画面から「編集中の図に戻る」で続きを編集できる
- ファイルへの保存は File System Access API のハンドルを覚えて上書きする。ハンドルも IndexedDB に入れるので、
  ブラウザを開き直しても同じファイルに上書きできる (書き込みの許可は最初の 1 回だけ取り直す)
- ドラッグ&ドロップは `getAsFileSystemHandle()` でハンドルを取り、ドロップした `.nmrfig` にも上書きできるようにする
- 保存先を覚えているときはボタンを「上書き保存」にし、状態 (ファイル名・未保存の変更・自動保存の時刻) を画面の下に出す

## 文献の図 (スクショ) から線幅を読む (lib/trace.ts)
- 列ごとに「いちばん上のインクの点」を拾って波形にする。ベースラインは高さの下から 25% の値
- SI の文の δ の位置で半値全幅を測り、信号ごとの線幅にする (simulate.ts の widthsHz)
- 山の幅が 3 画素より細いときは null にして、既定の線幅を使う (図の解像度＝線の太さしか見ていないため)
- 位置・多重度・J・H 数は SI の文を優先。図は「文から分からないもの」だけに使う

## 文献 (SI) のデータからスペクトルを作る
- 生データがない文献と比べるため、SI の文 (δ・多重度・J・H 数) を読んでローレンツ線で組み立てる (siParse.ts / simulate.ts)
  - 多重度は一次の分裂として二項係数で展開する。J が足りなければ最後の J を使い回す
  - 範囲で書かれた m は、その範囲に均した線の集まりにする。面積は H 数に比例させる
  - 線幅は半値全幅 (既定 ^1H 1.2 Hz、ほか 3 Hz)。実測の線形までは再現しない
- 実測と区別するため、色を変えて (紫) 名前を「文献値から作図 (著者 年)」にする。
  「文献 (著者 年)」だと論文に載っている実測スペクトルに見えるため (2026-09-23 に言い方を変えた)
- 引用元は必須で、図から消せない: タイトルの下に「文献値から作図: 全文」。layout.ts で行を必ず確保する。
  スペクトル名は書き換えられるが、この行は消せないので、実測でないことは必ず図に残る
- 貼られた引用は citation.ts で整える (貼り付け時と入力欄を離れたとき)
  - 改行・行末ハイフン・全角記号・先頭の文献番号を直し、DOI は `DOI: 10.…` に寄せる (`https://doi.org/`・`doi:` は外す)
  - 著者は筆頭のみ「姓, 頭文字」+ et al.。ACS 式・Wiley 式 (頭文字が先)・PubMed 式 (`Smith JA`) を読む
  - 誌名は表引き (正式名と略号の両方を鍵にする)。表になければうしろから略号らしい語を拾い、点を補う
  - 年・巻・頁は ACS 式・PubMed 式・(年) 式・IEEE 式を順に当てる。表題と号は落とす
  - **誌名と年が読めないときは作り直さない** (貼った文をそのまま図に出す)。整えたときは「元に戻す」を出す
  - 書き出しは ACS 風: 誌名と巻は斜体 `*{..}`、年は太字 `**{..}`、頁は最初の頁だけ + 「.」。DOI は図に出さない
  - 著者は ACS 式・Wiley 式・PubMed 式に加えて「Ka Wing Chan; …」のような綴りのままの並びも読む (`;` 区切りなら先頭の 1人)
  - 誌名の鍵からは of / the を落とす (「Journal American Chemical Society」のような貼り付けでも引ける)
  - 整えるのは貼り付けたときと「形を整える」を押したときだけ (手で直した文は触らない)
  - 読み取りは書式を外した文 (plainText) で行う。整えた文をもう一度整えても変わらないようにするため

## 印刷
- ツールバーの「印刷」で、今の図 (書き出しと同じ SVG) と測定条件の表を 1枚に出す
- 印刷用の中身は body 直下に portal で出す (@media print でアプリ本体を隠すため)
- 2026-09-24 本人の指定: **縦の用紙、上に図だけ、下に説明**。説明 = 図の名前 (保存したファイル名)・測定条件の表・印刷した日時。
  図の下にタイトルを出していない図だけ、説明にもタイトルを書く。図は幅いっぱい、縦は 150 mm まで (2D の正方形の図でも 1 枚に収まる)。
  `@page { size: portrait }` なので向きは縦に決まる (用紙の大きさは印刷の画面で選ぶ)
- 印刷のときは `body > :not(.print-view)` をすべて隠す。以前は `.app` の名前で隠していて、画面を作り直して名前が `.editor` に変わったとき
  2 枚目に編集画面が写っていた。背景は `:root` にも白を指定する (`:root` の灰色の背景が html より強い)。
  表のセルは画面側の `.note` とぶつかって崩れたので `print-note`
- 確かめ: `node scripts/print-check.mjs [出力.pdf] [demo]` (開発サーバー 5180 で ?demo の図を印刷用の中身にして PDF にする。UI_LANG=en で英語)

## 図に入れるもの
- 下のタイトル・軸の説明・外枠・枠・縦軸・スペクトル名は、右の「図に入れるもの」で切り替える (よく使うので折りたたまない)
- 外枠は図全体を囲む線 (1D・2D・推移グラフに共通、初期値は入れる)。プロットの囲みは別に「枠」で切り替える
- タイトルは一番下の見えているスペクトルから作る (核種・周波数・溶媒)。いま入る文字列をパネルに出し、「自動」を外せば自由に書ける
  (核種の違うスペクトルを並べたときは自動のタイトルが合わないので、混ざっていることを知らせる)

## SI 用テキスト
- 多重線の解析は一次の解析 (Hoye ら, J. Org. Chem. 2002, 67, 4014 の J-tree)。線の強度を整数にして合計が 2 のべき乗になるように丸め、
  左端から「まだ説明できていない一番近い線」までを J として木を作る。説明しきれなければ m
- 線の検出: 範囲の最大の 2.5% 以上かつノイズの 6 倍以上の極大。谷がほとんどない肩は線にしない
- 1 本線で半値全幅が 6 Hz を超えるものは br s。1H 以外の一重線は多重度を書かない
- 自動の信号検出 (1H): ノイズの 10 倍以上のピークを 20 Hz 以内でつなげ、両側に 8 Hz 足した範囲を積分にする。溶媒と不純物マーカーは除く
- 表で上書きした値 (多重度・J・H 数・帰属) は積分に保存し、プロジェクトファイルに残る
- 書式: `¹H NMR (400 MHz, C₆D₆[, 296 K]) δ 3.67 (t, J = 5.2 Hz, 18H[, 帰属])`。Word 用は HTML (sup/sub/i)、テキストは Unicode

## ホーム画面の測定のまとめ方
- Delta は生データを `〜-1-1.jdf`、処理して保存した版を `-1-2`, `-1-3` … で保存する
- サンプル名・測定開始時刻・次元・核種・実験名が同じファイルを1つの測定にまとめ、最新の処理版を代表にする
- ダブルクリック/ダブルタップは pointerup の間隔で判定する (タッチでは dblclick が来ない環境があるため)
- 確認はアプリ内のダイアログで行う (埋め込みブラウザでは confirm() が表示されないことがある)

## ロードマップ
| 段階 | 内容 | 状態 |
|---|---|---|
| Phase 1 | 1D読み込み・表示・重ね書き、不純物ラベル、マーカー、注釈図形、ppm値ラベル、基準合わせ、保存/読込、SVG/PNG出力 | 初版完成 (2026-09-17) |
| Phase 2 | スタイルのテンプレート保存、推移グラフ、PowerPoint の「図形に変換」への対応、図の拡大表示 | 完成 (2026-09-17) |
| Phase 2.5 | ホーム画面 (測定日ごとの一覧・プレビュー・スキーム・メモ・タグ)、積分、ドラッグで高さ変更 | 完成 (2026-09-17) |
| Phase 3 | SI 用テキストの自動生成 (多重線の解析)、ホーム画面の測定のまとめ・ダブルタップ | 完成 (2026-09-17) |
| Phase 3.5 | FID からの処理 (FT・位相補正・ベースライン補正・基準合わせ)、Word へのベクターのコピー | 完成 (2026-09-17) |
| Phase 4 | 2D (等高線 + 1D投影。COSY・HMBC を生データから処理) | 完成 (2026-09-18) |
| Phase 5 | 1ファイル版、2D の位相補正 (HSQC など) 、2D へのマーカー・図形 | |
